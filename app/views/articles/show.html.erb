<div class="container mt-4">
  <div class="card">
    <div class="card-body">
      <h1 class="card-title"><%= @article.title %></h1>
      <h6 class="card-subtitle mb-2 text-muted">Источник: <%= @article.source %></h6>
      <% if @article.link.present? %>
        <p><strong>Ссылка:</strong> <%= link_to @article.link, @article.link, target: '_blank' %></p>
      <% end %>
      <p><strong>Категория:</strong> <%= @article.category if @article.category %></p>
      <p><strong>Автор:</strong> <%= @article.user.email if @article.user %></p>
      <p><strong>Возрастная аудитория:</strong> <%= @article.age_group %></p>
      <p><strong>Дата добавления:</strong> <%= @article.created_at.strftime(date_format) %></p>

      <h2 class="mt-4">Содержимое новости</h2>
      <div class="article-content">
        <%= sanitize @article.html, tags: %w(h1 h2 h3 h4 h5 h6 p a img table tr td), attributes: %w(href src) %>
      </div>

      <div class="d-flex mt-3">
        <%= link_to 'Назад к списку новостей', articles_path, class: 'btn btn-secondary' %>
      </div>
    </div>
  </div>
</div>
